{% liquid
  graphql r = 'groups/search', limit: limit, page: 1, owner_id: owner_id, relation_name: relation_name, meta_visible: meta_visible, not_name: not_name, not_ids: not_ids, ask_to_join: ask_to_join

  return r.groups
%}
