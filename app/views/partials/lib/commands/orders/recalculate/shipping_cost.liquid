{% liquid
  case order.shipping_type
  when 'delivery'
    assign cost = order.delivery_fee | default: order.seller.delivery_fee
  when 'personal_pickup'
    assign cost = 0
  else
    assign cost = 0
  endcase

  return cost
%}
