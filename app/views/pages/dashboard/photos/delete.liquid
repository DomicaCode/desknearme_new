---
method: post
---
{% liquid
  graphql r = 'photos/delete', id: context.params.photo.id

  if r.errors
    assign params['valid'] = false
    assign params['errors'] = r.errors
  else
    assign params['valid'] = true
  endif

  return params
%} 